<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
/>
</head>
<body>
<nav class="px-10 py-8 flex items-center justify-between">
    <% var error = typeof error !== "undefined" ? error : "" %>
    <% if(error.length > 0){%>
        <div class="px-3 py-2 rounded-md bg-red-600 absolute top-[2%] left-1/2 -translate-x-[50%]">
            <h3 class="text-red-300"><%= error %></h3>
        </div>
    <%} %>
    <% var success = typeof success !== "undefined" ? success : "" %>
    <% if(success.length > 0){%>
        <div class="px-3 py-2 rounded-md bg-blue-600 absolute top-[2%] left-1/2 -translate-x-[50%]">
            <h3 class="text-white"><%= success %></h3>
        </div>
    <%} %>
    <h2 class="text-2xl font-black">Logo</h2>   
    <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
    <% if(loggedin){%>
        <div class="flex gap-8">
            <a class="font-medium" href="/profile">Home</a>
            <a class="font-medium" href="/create">Create New Post</a>           
            <a class="font-medium text-red-500" href="/logout">Logout</a>
        </div>
    <%} %>
  
</nav>